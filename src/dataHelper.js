const data = require("../data/data.json");

function removeDuplicates(list) {
    // Implement logic to remove duplicate objects from the list
    return [];
}

function getRandomList() {
    const lists = data.lists;

    // Implement logic to get a random list
    return [];
}

function getUniqueDataFromAllLists(){
    // Implement logic to get a unique list that combine all list's items
    return [];
}

module.exports = { removeDuplicates, getRandomList };
